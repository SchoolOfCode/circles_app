import React from "react";

export default function Timeline({ items, className, handleClick }) {
  const date = new Date();
  date.setHours(0, 0, 0, 0); //this makes sure that the date generated by the datepicker has a time of 0000 same as the date on the events data
  // console.log({
  //   date: date,
  //   datelength: date.length,
  //   eventDate: new Date("Thu Jan 19 2023"),
  //   eventdatelength: new Date("Thu Jan 19 2023").length,
  //   condition: date === new Date("Thu Jan 19 2023"),
  // });

  return (
    <div className={className}>
      <div
        className="flex flex-col justify-evenly top-60 left-50 mt-[7.5vh] h-[100%]"
        id="timeline-container-card"
      >
        {items.map((item) => {
          const eventDate = new Date(item.date);
          return (
            <div className="card w-96 h-36 bg-base-100 shadow-xl m-4">
              <div className="card-body flex flex-row justify-between">
                <div className="flex flex-col justify-between">
                  <div>
                    <div className="text-sm">
                      <h2 className="card-actions">{item.date}</h2>
                      <h2 className="card-actions font-bold">{item.time}</h2>
                    </div>
                  </div>
                  {date <= eventDate ? (
                    ""
                  ) : (
                    <button
                      onClick={handleClick}
                      className="decoration-black decoration-solid decoration-4 text-md border-4 border-[#BAE5F3] rounded-lg"
                    >
                      Add feedback
                    </button>
                  )}
                </div>
                <div className="flex flex-col">
                  <h2 className="card-title">{item.clubName}</h2>
                  <p>{item.eventName}</p>
                </div>
              </div>
            </div>
          );
        })}
      </div>
    </div>
  );
}
// bg-[#BAE5F3]
{
  /* <div className="flex justify-between">
                <h2 className="card-title">{item.clubName}</h2>
                  <h2 className="card-actions">{item.date}</h2>
                </div>
                <p>{item.eventName}</p>
                <div className="card-actions justify-end">
                  <div>
                  <h2 className="card-actions">{item.date}</h2>
                  </div>
                </div> */
}
